# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
*.tsbuildinfo

# Environment variables
.env
.env.local
.env.*.local

# CV-Git metadata
# Selective .cv/ ignoring - some files should be committed for portability
# See docs/STORAGE_FORMAT.md for details

# Ignore temporary/cache files
.cv/cache/
.cv/sessions/
.cv/embeddings/
.cv/delta_state.json

# Optionally ignore large vector files (uncomment if vectors are too large)
# .cv/vectors/

# DO NOT ignore these - they enable portable knowledge graphs:
# .cv/config.json      - repo configuration
# .cv/manifest.json    - storage manifest
# .cv/graph/           - graph nodes/edges (JSONL)
# .cv/documents/       - ingested markdown docs
# .cv/ingestion.jsonl  - document ingestion tracking

sync_state.json

# API keys and credentials (IMPORTANT - never commit these!)
*.key
!apt-repo/gpg.key  # GPG public key for apt repository (safe to commit)
*.pem
secrets/
credentials.json
config.local.json
.anthropic_key
.openai_key

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/

# Temporary files
tmp/
temp/
